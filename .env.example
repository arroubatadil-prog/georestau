# Example environment variables for AI proxy
# Put real keys in a local .env or server environment (DO NOT commit secrets)

# OpenAI API key (recommended)
OPENAI_API_KEY=sk-REPLACE_ME

# If you use Gemini on a server, set GEMINI_API_KEY (not implemented in proxy by default)
GEMINI_API_KEY=
